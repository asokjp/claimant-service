spring:
  application:
    name: claimantservice
  cloud:
    config:
      uri: http://35.202.142.5:32672
      name: claimantservice
    kubernetes:
      secrets:
        enabled: true
        paths: /etc/mongo/config
      reload:
        enabled: true
server:
  port: 8082
